---
title: 'Pipeline Configuration File'
draft: false
weight: 2
series: ["Gitlab-CI"]
series_order: 2
---

The pipeline configuration file will specify how the pipeline should run

It's name must be `.gitlab-ci.yml` and it must be placed in the root of the Gitlab repository that should be configured
